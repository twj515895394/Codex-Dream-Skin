# Project Handoff Archive

保存已经结束、被替代或仅用于历史追踪的项目级、Release 级和跨阶段 handoff。

规则：

- 历史文件不覆盖、不删除；
- 文件名保留日期、范围和状态；
- 当前会话入口始终是 [`../current.md`](../current.md)；
- 阶段内部快照优先放入 `../phases/phaseXX/archive/`；
- Release、重大迁移和跨阶段架构交接放入本目录。